You are an experienced front-end developer.

Task:
Create a single self-contained HTML file that implements an animated “printer printing paper” scene.

Technical Constraints:
- Use only HTML, CSS, and suitable JavaScript 
- Everything must be in a single .html file (or 2 to 3 files).
- The animation must run smoothly 
- Make the implementation such that the printing is controlled via scrolling. 
- Ensure that printing appears in order such that bottom is printed last and heading sections is printed first

Visual Requirements:
1. Render a square scene container centered on the page, designed to be for a printer
2. Inside the container, simulate a printer feeding a sheet of paper outward.
3. As the paper moves out, content appears line-by-line on the paper (text or simple shapes). 
4. When the page is fully printed:
   - The scrolling must reach the end of page.
5. When scrolling back, the page/paper must retract to go back into the printer.

Thinking Requirements:
- Ensure that you make the printing smooth and logical 

Future Work:
- The idea for this could be to render a CV and act as personal portfolio website

Implementation Notes:
- Use CSS animations or requestAnimationFrame where appropriate.
- Keep the code clean and well-commented.
- Use simple but clear visuals (printer slot, moving paper, printed lines).
- Ensure the layout is responsive and centered vertically and horizontally.

Output:
Return the complete HTML files with embedded CSS and JavaScript.

Ask questions where necessary. Let me then test it